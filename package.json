{
  "name": "goswamipara-mansa-mata-club",
  "version": "1.0.0",
  "private": true,
  "description": "Official website & CMS for গোস্বামীপাড়া মনসামাতা ক্লাব (Goswamipara Mansa Mata Club) — MERN stack",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm start --prefix client",
    "seed:admin": "npm run seed:admin --prefix server",
    "build": "npm run build --prefix client",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "test:api": "node tests/api.smoke.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
